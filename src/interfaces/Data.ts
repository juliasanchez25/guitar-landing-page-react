export type Data = {
  id: number;
  text: string;
  subText: string;
  description: string;
  swatchColor: string;
  background: string;
  textColor: string;
  buttonColor: { text: string; background: string };
};
